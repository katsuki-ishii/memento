# イベント編集ダイアログ要件

- 目的: 週に紐づくイベントの登録/編集/削除。
- フィールド: title, note, mood(選択), weekId 表示（誕生日起点の週）。
- 操作: 保存=POST/ PATCH、削除=DELETE、キャンセル=閉じる。
- トリガー: ダッシュボードのセルクリック。
- バリデーション: title/note の長さ制限、weekId は変更不可。
- 認証: 必須。
- スタイル: モーダル、白黒基調、アクセント最小。
