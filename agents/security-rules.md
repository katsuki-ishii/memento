# security-rules.md

- 秘密情報（トークン/キー/クレデンシャル）を Git に含めない。`.env.local` のみローカル。
- 最小権限の IAM ポリシーを使用し、不要になったキーは破棄。
- フロントは Cognito PKCE 前提。アクセストークンを永続保存しすぎない。必要ならセッション更新を適切に行う。
- CORS は必要なオリジンのみ許可。エラーメッセージに内部情報を含めない。
- ユーザー入力は検証し、XSS/CSRF を意識（フォームでエスケープ、API はオリジン制御）。
